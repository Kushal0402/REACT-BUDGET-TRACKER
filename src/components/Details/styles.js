import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    income: {
        borderBottom: '10px solid turquoise',
    },
    expense: {
        borderBottom: '10px solid orange',
    },
}));